# Адрес твоего API-сервера (VPS с HTTPS, без завершающего слэша)
VITE_API_URL=https://kiko-api.duckdns.org

# Публичный VAPID-ключ (из `npx web-push generate-vapid-keys`)
# Приватный ключ сюда НЕ попадает — он живёт только в server/.env на VPS.
VITE_VAPID_PUBLIC_KEY=B...
